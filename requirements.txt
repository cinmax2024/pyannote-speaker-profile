runpod==1.7.9
numpy<2
# Pin the torch stack to the base image (runpod/pytorch:2.4.0-py3.11-cuda12.4.1).
# Unpinned, pip upgrades torch to latest (2.12) while torchvision stays +cu124
# built for 2.4 — pyannote.audio then crashes at import (circular torchvision
# import) and every worker exits at job time.
torch==2.4.0
torchaudio==2.4.0
torchvision==0.19.0
pytorch-lightning==2.4.0
pyannote.audio==3.1.1
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1
huggingface-hub<1
flask==3.0.3
speechbrain==1.0.2
